#include <uiMain.h>

int main()
{

  start(0, NULL); 
  // FILE *arq;
  // arq = fopen("utils/input_image_example_Gray.txt", "r");
  // if(arq != NULL){
  //   printf("Abriu!\n");
  // }
  // ImageGray *imgGray = read_image_gray_from_file("utils/input_image_example_Gray.txt");

  // if (imgGray == NULL)
  // {
  //   fclose(arq);
  //   return 1;
  // }else{
  //   printf("Deu certo!\n");
  // }

  // printf("%d - %d\n", imgGray->dim.altura, imgGray->dim.largura);
  
  // Operações para ImageGray
  // ImageGray *flipped_vertical = ImageGray *flip_vertical_gray(imgGray);
  // for (int i = 0; i < imgGray->dim.altura ; i++)
  // {
    
    
  // }
  
  // ImageGray *flipped_horizontal = ImageGray *flip_horizontal_gray(imgGray);
  // void free_image_gray(imgGray);

  // ImageRGB *create_image_rgb(largura, altura);
  // void free_image_rgb(image);
  // ImageRGB *flip_vertical_rgb(image);
  // ImageRGB *flip_horizontal_rgb(image);
  // start(0, NULL);
  return 0;
}